name: Calendar Polling Cron

on:
  schedule:
    # Run every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  poll-calendar:
    runs-on: ubuntu-latest
    
    steps:
    - name: Poll Calendar for Events
      run: |
        # Call the calendar polling cron function
        curl -X POST \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
          -H "Content-Type: application/json" \
          "${{ secrets.SUPABASE_URL }}/functions/v1/calendar-polling-cron"
      
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

    - name: Handle failure
      if: failure()
      run: |
        echo "Calendar polling failed"
        # Add notification logic here if needed

    - name: Handle success
      if: success()
      run: |
        echo "Calendar polling completed successfully" 