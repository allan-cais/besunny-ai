# Minimal requirements for Railway deployment
# Only essential dependencies to get the backend running

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Core utilities
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Basic middleware
python-multipart==0.0.6

# Logging
structlog==23.2.0

# HTTP client
httpx==0.25.2
