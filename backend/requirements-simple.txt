# Minimal requirements for BeSunny.ai Backend
# Only essential packages to get the build working

# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Essential utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
email-validator>=2.1.0

# Database (essential)
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.0

# Supabase (critical)
supabase>=2.0.0

# Security and authentication (critical for JWT handling)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2

# HTTP and networking
httpx>=0.25.2
requests>=2.31.0

# Middleware and utilities
python-multipart>=0.0.6

# Basic utilities
structlog>=23.2.0

# Redis (if needed)
redis>=5.0.1

# Additional utilities that might be needed
celery>=5.3.0
